## Can you create a code in python that takes an array of timestamps and creates one continuous horizontal bar with the timestamps proportionally marked on the x-axis?

pip install matplotlib

import matplotlib.pyplot as plt
import numpy as np

# Example array of timestamps
timestamps = [
    "0:00", "0:10", "0:15", "0:50", "1:12", 
    "1:29", "1:45", "2:18", "2:36", "2:54", 
    "3:14", "3:33", "3:45", "4:11", "4:51"
]

# Convert timestamps to seconds
def convert_to_seconds(timestamp):
    minutes, seconds = map(int, timestamp.split(':'))
    return minutes * 60 + seconds

# Create an array of seconds
seconds = [convert_to_seconds(ts) for ts in timestamps]

# Create a continuous horizontal bar
plt.figure(figsize=(10, 2))
plt.hlines(1, seconds[0], seconds[-1], color='skyblue', linewidth=5)

# Mark the timestamps on the x-axis
plt.xticks(seconds, timestamps, rotation=45)
plt.yticks([])  # Hide y-ticks
plt.xlabel('Time (MM:SS)')
plt.title('Continuous Timeline of Timestamps')
plt.grid(axis='x')

# Show the plot
plt.xlim(seconds[0], seconds[-1])  # Set x-axis limits
plt.show()
